import ResponsiveDrawer from "@/app/components/userSideBar";
import React from "react";

const Settings = () => {
  return (
    <div>
      <ResponsiveDrawer selected="Settings" />
    </div>
  );
};

export default Settings;
